<h2>Restaurants:</h2>

<ul>
  <% @restaurants.each do |restaurant| %>
  <li>
    <%= link_to restaurant.name, restaurant_path(restaurant) %>
  </li>
  <% end %>
</ul>

<p><%= link_to "Create new Restaurant", new_restaurant_path, {class: "btn btn-primary"} %></p>
